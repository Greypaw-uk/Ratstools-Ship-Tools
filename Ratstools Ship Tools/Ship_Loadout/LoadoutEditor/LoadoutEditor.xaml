<Page x:Class="Ship_Loadout.LoadoutEditor.LoadoutEditor"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Grid.Row="0">
            <Button Name="btn_new" Content="New" Width="50" Margin="10" Click="Btn_new_OnClick" Style="{DynamicResource btn_orange}"/>
            <Button Name="btn_load" Content="Load" Width="50" Margin="10" Click="Btn_load_OnClick" Style="{DynamicResource btn_orange}"/>
        </StackPanel>

        <Frame Name="shipFrame" Grid.Row="1"/>
    </Grid>
</Page>
